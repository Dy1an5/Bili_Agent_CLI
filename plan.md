# Bili Agent CLI 统一视频模型与内容数据库重构计划

> 必须完整实现并验证一个步骤、将对应复选框打勾后，才能开始下一步骤。不得提前实施后续步骤。

## 实施清单

- [x] **步骤 1：建立并提交 `plan.md` 执行清单**
  - 写入本计划、强制勾选门禁和每步验收要求。

- [x] **步骤 2：彻底移除 FastAPI**
  - 删除 FastAPI 应用入口、`routes/` 和 HTTP 路由测试。
  - 移除 FastAPI 依赖并更新 `uv.lock`；保留 `httpx`。
  - README 改为 CLI/Agent 使用方式，清理 FastAPI 专属描述。
  - 验收：源码、测试、依赖和 README 无 FastAPI 残留，现有非 HTTP 测试通过。

- [x] **步骤 3：建立统一视频领域模型**
  - 定义 `VideoIdentity(bvid, cid)`、作者、详情、视频反馈和五类来源上下文。
  - 各 B 站接口保留自己的解析和分页外层，其中视频转为同一 `VideoRecord`。
  - 验收：全部视频来源输出相同视频 JSON 形状，缺失上下文为 `null`。

- [x] **步骤 4：实现 CID 补全服务**
  - 已有合法 CID 时直接使用；否则先查已确认默认 CID，再请求 `/x/web-interface/view`。
  - 多 P 稿件使用 `data.cid`，不展开其他分 P。
  - 单项失败记录 ingestion failure，其他项继续。
  - 验收：覆盖直接 CID、缓存 CID、详情补全、多 P 和部分失败。

- [x] **步骤 5：创建独立内容数据库**
  - 使用 `privacy/content.db`，目录权限 `0700`、文件权限 `0600`，开启外键和 schema version。
  - 建立视频、反馈、各来源关系、失败记录和同步状态表。
  - Upsert 时新 `null` 不覆盖旧非空值，零是有效值。
  - 验收：数据库结构、权限、事务、复合主键和多收藏夹关系测试通过。

- [x] **步骤 6：接入“读取即入库”**
  - 六类 Agent 读取工具执行“请求→规范化→CID 补全→事务入库→返回”。
  - 外层继续保留分页、游标、收藏夹或搜索条件。
  - 新 `source_id` 含 CID，兼容旧 BVID-only 会话和收藏确认。
  - 验收：六类工具都在读取后产生正确数据库记录。

- [x] **步骤 7：预留评分数据边界**
  - 建立 `topics`、`video_topics`、`user_content_events`、`topic_preference_scores` 空表。
  - 定义主题候选、行为事件和评分结果模型。
  - 不调用大模型、不生成主题、不计分、不改变推荐排序。
  - 验收：表和模型可初始化，当前 Agent 行为不受影响。

- [x] **步骤 8：更新文档并完成全量验收**
  - README 说明统一字段、`null` 语义、复合身份、部分成功和 `content.db` 隐私边界。
  - 说明首期仍实时请求 B 站并入库，不做自动刷新或数据库优先读取。
  - 验收：`uv run pytest` 通过，全仓无 FastAPI 残留，执行清单全部勾选。

## 已锁定默认

- BVID、CID、AID、MID 都使用字符串。
- 多 P 列表项只绑定详情接口的默认 CID。
- 首期覆盖关注动态、指定用户动态、收藏、稍后再看、历史和搜索。
- 每次工具仍请求 B 站后入库；不增加定时器、`sync` 命令或缓存优先读取。
- `content.db` 与 `memory.db` 分离，不读取或泄露 `secret/` 内容。
